-- Grug find, grug replace, grug happy!!
-- ast/ripgrep inspired search and replace for variables

return {
    'MagicDuck/grug-far.nvim',
    config = function()
      require('grug-far').setup({});
    end
}
