-- Highlight the TODO comments to make them super pretty
-- It also compiles them into a list but who cares about that
return {
  "folke/todo-comments.nvim",
  dependencies = { "nvim-lua/plenary.nvim" },
  opts = {}
}
